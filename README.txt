#After installing Node. Run this to download Angular CLI
>npm install -g @angular/cli
#Initialize new node app
>ng new my-first-app
>	
#Initialize server
>ng serve
#If you want you can change the port used
>ng serve --port 1234
>npm install --save bootstrap@3

#Creating components
>ng generate component servers
>ng g c servers